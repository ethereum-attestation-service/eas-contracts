import { describeDeployment } from '../../test/helpers/Deploy';

describeDeployment(__filename, () => {
  it('should name initial schemas', async () => {
    // Since initial schemas naming is optional, there is no need to test it explicitly.
  });
});
